# Talyn — Claude Context

Talyn is a desktop "mission control" app for **GitHub PR management**, powered by **cloud coding agents**. It tracks your open/review-requested PRs in a prioritized GitHub panel, and delegates fix/respond/review work to cloud providers that run the agent loop on their own sandbox and open a PR. **Talyn Fleet (`selfhosted`) is the default** — Firecracker microVMs on our own hardware, running the workspace's **own Claude or Codex subscription** — with **PostHog Code** as the fall-back. Codex Cloud is deferred; Claude Code was removed.

**As of the cloud-only refactor (June 2026)** the app no longer runs anything locally: the bundled daemon, local/remote environments, in-process Claude agents, permission gates, backlog/continuous-build, and the per-task git working tree are all gone. Every task is a cloud task. See [`docs/CLOUD_PROVIDERS.md`](./docs/CLOUD_PROVIDERS.md).

**Target user**: engineers who live in GitHub PRs and want to hand routine PR work to cloud agents.

## Git Workflow

**Repository**: `git@github.com:Gilbert09/talyn.git` (main branch)

After completing each task: stage relevant files, commit with a descriptive message, push to main. No branches or PRs for Talyn itself. Keep commits focused and atomic.

**Commit authorship**: commits should be authored by Tom directly. Do NOT append `Co-Authored-By: Claude …` trailers or any other AI-attribution lines to commit messages in this repo.

## CI & Releases (`.github/workflows/`)

Every push to main deploys — treat a push as a production release. All deploy/publish workflows are fork-guarded with `if: github.repository == 'Gilbert09/talyn'` (these guards compare against the CURRENT repo name; update them if the repo is ever renamed, in the same push, or deploys silently skip).

- **`test.yml`** — every push + PR. 3-OS matrix (macOS/Windows/Ubuntu): full builds → `typecheck` → `lint` → `npm test`. The only gate — nothing blocks a deploy on it, so don't push red.
- **`deploy-backend.yml`** — push to main touching `packages/backend|shared`, `Dockerfile`, `railway.toml` (+ `workflow_dispatch`). Deploys to Railway via CLI token; cutover is health-gated (`/health` does a real DB check and 503s while draining), so a boot-refusing build keeps the old one serving. NOTE: every deploy briefly overlaps old+new instances — the pg advisory locks (`services/advisoryLock.ts`) exist for exactly that window.
- **`deploy-marketing.yml`** — push to main touching `apps/marketing/**`. Lint + typecheck gate, then Vercel prebuilt deploy to www.talyn.dev.
- **`publish.yml`** — the **stable release**, cut **every six hours** and on demand. The scheduled run is skipped when main has not moved since the latest stable release (`/releases/latest` + the compare API, so a run the cron misses is caught up by the next one). On demand: Actions → Publish → "Run workflow" (version input optional; empty auto-picks the next patch, and electron-builder creates the release AND the tag, so no local git needed) or push a `vX.Y.Z` tag. Builds **macOS arm64+x64 (signed + notarized), Windows NSIS, and Linux AppImage**, publishing a **full release**, which is what the updater and the talyn.dev download button follow. The tag/input is the single source of truth for the app version (baked into `release/app/package.json` at build time, never committed). A `concurrency` group queues a manual dispatch behind a scheduled run: the `version` job reads the release list at run time, so two runs in flight would stamp the same version. (`nightly.yml`, which shipped arm64-only pre-releases on the same cron to nightly-channel users only, was folded into this in Session 96.)

  **Job shape**: a `version` job resolves the version ONCE and fans it out (three legs each computing "next patch above the latest release" would race), then the **macOS leg runs alone** — it's what creates the GitHub Release (and the tag, on the schedule and dispatch paths) — and only then does a `windows-latest`/`ubuntu-latest` matrix upload into it. Keep that ordering: parallelising all three races three electron-builder processes to create the same release, and chaining means a Windows/Linux failure can't take down a macOS release that already published. **Windows ships unsigned** until an EV cert is bought (SmartScreen warns on first install); setting `CSC_LINK`/`CSC_KEY_PASSWORD` fixes it with no workflow change.

**Update channels**: the desktop picker (Settings → About; persisted in userData via `src/main/updateChannel.ts`, default `stable`) maps to electron-updater's `allowPrerelease`. Since Session 96 nothing publishes a pre-release, so both channels receive the same stable build. The picker is kept so a pre-release track can come back without a client change; removing it (and the "every build as it lands" copy) is an open follow-up.

## Testing — run only the relevant tests while iterating

**Do NOT run the whole test suite on every change.** It's slow (many backend suites spin up a real pglite Postgres per file) and wastes the loop. Run only the tests that cover what you actually touched, picked by what the change can plausibly break — not by habit:

- **Backend** (`packages/backend`, Vitest): `npx vitest run <path/to/file.test.ts>` for the specific file(s); add more paths or a glob (e.g. `npx vitest run src/__tests__/prMonitor*`) when a change spans a few related suites; use `-t "<name>"` to target a single `describe`/`it`.
- **Desktop** (`apps/desktop`, Jest): `npx jest <pattern>` for the matching test(s).

Run the **full** package suite (`npm test`) only when wrapping up a change, or when the edit is genuinely cross-cutting — shared types (`packages/shared`), a widely-imported helper, or DB schema/migrations. Always pair the run with `tsc --noEmit` + `eslint` on the changed files. See [`docs/TESTING.md`](./docs/TESTING.md) for the broader strategy.

## Where Things Live

- **[`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md)** — system diagram, tech stack, core concept details, key decisions, resolved questions
- **[`docs/ROADMAP.md`](./docs/ROADMAP.md)** — full phased TODO (Phase 1–20), backlog, known gaps, full priority queue
- **[`docs/SESSIONS.md`](./docs/SESSIONS.md)** — chronological session notes
- **[`docs/CLOUD_PROVIDERS.md`](./docs/CLOUD_PROVIDERS.md)** — the cloud task provider abstraction (registry, per-provider modules, roadmap)
- **[`docs/QUALITY_PARITY.md`](./docs/QUALITY_PARITY.md)** — desktop polish/parity assessment vs Conductor; what's done + prioritized backlog (feed perf, PR diffs/merge, composer, tests)
- **[`docs/INCREMENTAL_CHECK_COUNTS.md`](./docs/INCREMENTAL_CHECK_COUNTS.md)** — webhook-driven incremental check counting design
- **[`docs/MCP_SERVER.md`](./docs/MCP_SERVER.md)** — the `@talyn/mcp-server` package
- **[`docs/SETUP.md`](./docs/SETUP.md)** — env vars / account setup
- **[`docs/TESTING.md`](./docs/TESTING.md)** — testing strategy + coverage

When a session lands non-trivial work, append a note to `docs/SESSIONS.md`. When a phase item changes status, update `docs/ROADMAP.md`. When a decision is revisited, update `docs/ARCHITECTURE.md`.

## Core Concepts (at a glance)

- **Workspace** — groups related repos + integrations (e.g., "PostHog" = `posthog/posthog` + `posthog/posthog.com` + `posthog/charts`). **Every owner always has at least one**: `services/workspaceBootstrap.ts` mints a `DEFAULT_WORKSPACE_NAME` one on `GET /workspaces` (the call every client makes on boot), advisory-locked per owner so concurrent first-loads cannot double-insert. Onboarding therefore opens on **Connect GitHub**, not on naming a workspace. **Consequence to respect: a workspace existing no longer means the user is set up** — both front ends read `Workspace.integrations.github` to decide "already onboarded", because keying off `workspaces.length > 0` would skip the wizard (and its required GitHub step) for every new user. See Session 106 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- **Cloud provider** — a vendor that runs the whole agent loop on its own sandbox and opens a PR. Pluggable behind `CloudTaskProvider` (`packages/backend/src/services/cloudProviders/`): a registry + per-provider `dispatch`/`reconcile`/credentials. **Talyn Fleet** (`selfhosted`) heads `CLOUD_PROVIDER_ORDER`, **PostHog Code** is the fall-back, **Codex Cloud is deferred** (no server-to-server API), and **Claude Code was removed** (metered API credits only — the fleet runs Claude on the user's own subscription instead). See [`docs/CLOUD_PROVIDERS.md`](./docs/CLOUD_PROVIDERS.md).
  - **The fleet is one provider with TWO agents**, and the **model carries the vendor**: `fleetProviderForModel` reads the model id, and the fleet builds the microVM's egress route table from it, so a Codex run has no route to `api.anthropic.com` at all. Picking an agent per task is picking a model — a second field would be a second source of truth that can disagree.
  - **A dispatch always sends the workspace's own key for the vendor it runs, and suppresses the other with `policy.credentials`.** The gateway fills an *absent or blank* key from its tenant's sealed custody, so `?? ''` was a silent route to spending someone else's subscription; no credential is a **refusal**. `cloudTask.extra.llm` records the vendor, and `poller.recredential` + `resolveRunCredentials` must both read it — they used to send the Claude key unconditionally.
  - `FLEET_ENABLED` + `FLEET_ALLOWED_EMAILS` still gate it (unset = nobody). A non-allow-listed workspace has the fleet dropped from its chain and lands on PostHog Code, exactly as before.
- **Environment** — now just a **secret-free marker**, one auto-provisioned row per connected cloud provider. Its `type` (a `CloudProviderType`) is how a task resolves its provider; per-workspace credentials live on the `integrations` row. No daemon, no pairing. PostHog Code's row carries EITHER an encrypted personal API key or an encrypted OAuth token pair, decided by `config.authMethod` (absent = key), and everything downstream reads one `getToken()` from `posthogCode/credentials.ts` rather than branching — see Session 81.
- **Task** — the unit of work, always delegated to a cloud provider. Types: `code_writing` (freeform prompt on a repo), `pr_response`, `pr_review`. Lifecycle: `queued` → `in_progress` → `completed`/`failed`. The cloud poller (`cloudProviders/poller.ts`) drives status + ingests the transcript; review happens on the provider's PR (no local `awaiting_review` gate).
- **Workflow** — user-defined PR automation, behind `WORKFLOWS_ENABLED` + `WORKFLOWS_ALLOWED_EMAILS` (unset = nobody, the `FLEET_*` shape). A named, workspace-scoped rule: *on these PR lifecycle events, matching these conditions, do these actions* (labels, reviewers, assignees, a comment, a skill or prompt run, add to My PRs, add to the merge queue). Vocabulary + matcher + validator in `packages/shared/src/workflows.ts` (the `prFilters.ts` argument, higher stakes — a workflow comments and merges); engine in `packages/backend/src/services/workflows/`; tables `workflows` + `workflow_runs` (migration `0052`). **Four things to respect:**
  - **It reads the webhook PAYLOAD, never a `pull_requests` row**, which is what lets it fire on every PR in a watched repo including untracked ones and other people's. Hooked into `processWebhookDelivery` ABOVE the `isRefreshEvent` gate (that predicate is about refreshes, a narrower question). Where a payload is short of a fact — an `issue_comment` describes an *issue*, so no base branch / head branch / draft; a `check_suite`'s PRs are `{number, base, head}` — facts carry `unknownFields`, a condition on one **fails** rather than passing, and the engine enriches from the row first (SQL accessors into `last_summary`, never the blob).
  - **`UNIQUE (workflow_id, delivery_id)` is the concurrency design.** The run row is inserted BEFORE any action runs; a conflict means a redelivery or another replica already owns it. No advisory lock. Settling is TWO writes — outcome first, then the task/PR links best-effort — because one combined UPDATE fails the FK when a linked row has gone and strands the run at `running`.
  - **Two loop guards, not a quota.** Self-echo suppression (skip a delivery whose actor is Talyn's own App, on the events our actions produce — App only, never the connected user; and `pr_merged` deliberately excluded) plus `max_runs_per_pr_per_hour` (default 5) counted from `workflow_runs`, with `skipped` rows excluded from the count and the refusal announced once per window.
  - **The merge action is the QUEUE, not `mergePullRequest`.** A direct merge 405s on a gated base. This is why the enqueue path moved out of `routes/pullRequests.ts` into `services/mergeQueue/membership.ts` (per-PR `applyQueueMembership` vs per-call `setQueueMembership`). `local:` skills are refused at save time — the backend cannot read `~/.claude/skills`. `GET /features` → `{ workflows }` decides only what to DRAW; every route, the engine and the task actions gate independently. See Session 117 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- **Operator console** — `apps/admin` at admin.talyn.dev. Cross-tenant by definition; gated by `users.is_admin` + `requireAdmin`, which is the whole permission model. Its API is `/api/v1/admin` (pre-`ownerScope`). Every mutation requires a reason and writes to `admin_audit_log`.
- **Billing** — free plan = **3 active tasks** (`pending|queued|in_progress`) **and 3 merge-queue PRs** per owner across all their workspaces; **Unlimited** = $15/mo (or $150/yr) via **Polar** (merchant of record). The provider-agnostic entitlement seam is `services/billing/entitlements.ts` (task gate in `createCloudTask` + the retry/start/PATCH re-activation paths, `TaskLimitError` → 402 `code:'task_limit_reached'`; merge-queue gate in `POST /pull-requests/:id/merge-queue`, `MergeQueueLimitError` → 402 `code:'merge_queue_limit_reached'`; both open the desktop UpgradeModal via `maybeHandleBillingLimit`); Polar specifics live only in `services/billing/{polar,webhook}.ts` (webhook: `/api/v1/webhooks/polar`, raw-body, idempotent + order-safe). Enforcement runs ONLY when the all-or-nothing `POLAR_*` env group is set (absent = limits off — the dev default and the prod kill switch). **Both gates now run unconditionally — there is no per-caller exemption.** `services/billing/clientGate.ts` used to wave through any client identifying as a build older than the release that shipped each paywall UI; it was deleted in Session 104 once the only thing still claiming it was an unstamped LOCAL build reporting the `0.1.0` placeholder from `release/app/package.json` (see `apps/desktop/.erb/configs/appVersion.ts`, which now reports `dev` instead). `X-Talyn-Client-Version` is still sent, but nothing in billing reads it. **The gate you cannot see is the watchers**: a merge-queue or auto-keep fix run that hits the task cap is deferred server-side (`deferred_task_limit`), not 402'd — there is no request to answer, so no UpgradeModal and no `paywall_shown`. For a merge-queue-heavy user that is the *dominant* path, and it is why the paywall reads as never firing. **A third gate is a FEATURE gate, not a cap**: turning ON the workspace default "keep new PRs green" (`settings.defaultAutoKeepMergeable`) needs Unlimited — `AutoKeepDefaultPlanError` → 402 `code:'auto_keep_default_requires_unlimited'`, asserted in the workspaces PATCH on the OFF→ON **transition only**, which is what grandfathers a free workspace that already has it on (turning it off gives that up). The modal's pitch is derived from usage, so the billing store carries an `upgradeReason` and the feature branch is checked before the usage ones. Comp an account with `UPDATE users SET plan_override='unlimited' WHERE email='…'` — webhooks never touch that column. See Sessions 68 + 70 + 104 + 105 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- **GitHub/PR core** — `services/{github,githubGraphql,prMonitor,prCache,prFocus}.ts` + `routes/{github,pullRequests,repositories}.ts` + the desktop GitHub panel / PR pills / detail sheet. This is the heart of the app. (The standalone **Inbox** — a prioritized queue of PR items needing attention — was removed; PRs needing attention surface directly in the GitHub panel's "Needs attention" / Mine / Review buckets.)

See [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) for the full treatment.

## Debug Tooling — keep it current

The **Debug** panel now lives ONLY on the operator console (admin.talyn.dev → Ops → Debug). It was removed from `apps/web` and `apps/desktop` in Session 80: it streams backend internals across every account, so it belongs on an admin-gated surface rather than in the product. It surfaces app internals live: outbound HTTP, poll-loop ticks, WebSocket traffic, and domain events. It's powered by an in-process `debugBus` (`packages/backend/src/services/debugBus.ts`, ring buffer + counters + poller registry) that records metadata only (URLs are query-stripped; no headers/bodies/tokens) and streams over the existing WS as `debug:event`. UI lives in `apps/admin/src/components/panels/DebugPanel.tsx` (one copy — the desktop/web duplicates are gone).

**When you add or change a subsystem, wire it into the bus so the panel stays honest:**
- **New outbound HTTP** (a new external API/integration) → time the call and `debugBus.recordHttp({ service, method, url, status, durationMs, ok, error? })` at the central request funnel (see `github.ts` `apiRequest`/`executeGraphql`, `posthogCode/client.ts` `request`). Add a one-liner to `SERVICE_INFO` in `DebugPanel.tsx`.
- **New poll loop** → `debugBus.registerPoller(name, intervalMs, description)` in `init()` (the `description` arg is required — that's the tooltip) and `debugBus.pollerTick(name, { durationMs, ok, error? })` in the tick's `finally`.
- **New WebSocket message/broadcast or domain event** → `debugBus.recordWs(...)` / `debugBus.recordEvent(...)`. If it's a new outbound broadcast type, keep the `event.type !== 'debug:event'` loop-guard in `websocket.ts` intact.
- **New `DebugCategory`** → extend the shared type, `CATEGORY_INFO`, `CATEGORY_LABEL`, `categoryClasses`, the filter chips in `DebugPanel.tsx`, **and the `CATEGORIES` allowlist in `routes/debug.ts`** — an unlisted value there is not rejected, it silently falls through to "no filter", so the chip appears to do nothing rather than to fail. (`db` and `webhook` were missing for exactly that reason until Session 80.)

**GraphQL budget cards** ("GraphQL budget" row in the panel) show GitHub's per-account GraphQL points budget (`inst:<id>` for an App installation, else login), fed by `services/graphqlBudget.ts`. The budget is read off the free `rateLimit { limit cost remaining resetAt }` field spliced into every batched query (`githubGraphql.ts` `RATE_LIMIT_FIELD`); `github.ts` `executeGraphql` captures it via `graphqlBudget.record(accountKey, …)`. The tracker is **pure / debug-bus-independent on purpose** — it also drives a proactive deferral: the reconcile sweep (`prReconcileSweep.ts`) calls `graphqlBudget.shouldDefer(accountKey)` and skips an account whose remaining points are in the reserve (`RESERVE_POINTS`), so webhooks / merge queue / manual refresh keep flowing until the window resets. `debugBus.snapshot()` just reads `graphqlBudget.snapshot()` for display. Tests: `graphqlBudget.test.ts`.

Tests live in `packages/backend/src/__tests__/debugBus.test.ts` — extend them alongside changes.

## Database Egress — keep queries lean

The backend runs against Supabase Postgres and we pay for DB egress (result-row bytes shipped DB→backend). A bare Drizzle `.select()` is `SELECT *` — it ships **every** column, including large jsonb blobs the caller usually doesn't touch. The two expensive columns are **`tasks.transcript`** (the cloud-run conversation log, often MBs) and **`pull_requests.lastSummary`** (~2KB, but multiplied across every tracked PR on the poll loops). The DB-egress tile in the Debug panel (fed by `instrumentEgress` in `db/client.ts`, which records per-query `bytes`/`rows`/`table`) is how you spot regressions — watch it after touching any read.

**Rules of thumb when writing or reviewing a query:**

- **Never `.select()` (= `SELECT *`) unless the caller genuinely uses every column.** Default to an explicit column list. This is most critical on anything that (a) runs in a poll loop or per-request hot path, or (b) reads a table with a large jsonb column (`tasks`, `pull_requests`, `workspaces.logo`, `integrations.config`).
- **Reuse the established projection helpers — don't invent new shapes:**
  - `services/taskSerialize.ts` → `taskColumnsNoTranscript` (every `tasks` column except `transcript`) + `rowToTask(row, { includeTranscript? })`. Any task read that doesn't render the transcript should use this. Only `GET /tasks/:id` and `POST /tasks/:id/message` select the full row.
  - For poll-loop / hot-path reads on `pull_requests`, define an `as const` projection object next to the consumer and type the row as `Pick<typeof table.$inferSelect, keyof typeof PROJECTION>`. Existing examples: `QUEUE_COLUMNS` (`mergeQueueProcessor.ts`), `WATCH_COLUMNS` (`prAutoMergeWatcher.ts`), `PR_CACHE_COLUMNS` (`prCache.ts`), `BROADCAST_COLUMNS` (`mergeQueueBroadcast.ts`), `PR_LOOKUP_COLUMNS`/`PR_FLAG_COLUMNS` (`routes/pullRequests.ts`), `CLOUD_ENV_COLUMNS` (`taskQueue.ts`). The `Pick` type is the regression guard — `tsc` fails if a consumer later reads a column the projection drops, so it can never silently re-bloat.
- **If you only need a scalar/boolean derived from a big jsonb, compute it in SQL — don't fetch the blob.** Use a `sql<...>` expression so the column never ships. Precedents: `cloudProviders/poller.ts` derives `transcriptEmpty` with a `CASE … jsonb_array_length(transcript) …`; `prMonitor.fastPollWorkspace` derives the in-flight check count with `COALESCE((last_summary -> 'checks' ->> 'inProgress')::int, 0)` instead of selecting `lastSummary`. When you do this, **pin the SQL to the JS semantics it replaces with a pglite test** (see `cloudPollerEgress.test.ts`, `prMonitorFastPollEgress.test.ts`) — keep the JS helper exported as the canonical definition the SQL must match.
- **Don't fetch a column to read it once for a rare branch.** If a loop reads N rows but only needs an expensive column for the few that hit a condition (e.g. `reconcileRelationshipFlags` only needs `lastSummary` for rows whose flags changed), drop it from the bulk select and re-fetch it per-row inside the branch — N blob fetches/tick become K (usually 0).
- **The same discipline applies to what leaves the backend.** WS broadcasts and REST responses should serialize a crafted shape, never a raw full row (see `emitPullRequestUpdated` / `rowToPublicShape`). Don't echo `transcript` or unread jsonb to the desktop.

When in doubt, add a `.toSQL()` assertion (`expect(query.toSQL().sql).not.toContain('transcript')`) — it proves the projection excludes the blob without a live DB (see `projectionEgress.test.ts`).

## Active Priorities

> Full list in [`docs/ROADMAP.md`](./docs/ROADMAP.md). The active direction is the cloud-provider abstraction in [`docs/CLOUD_PROVIDERS.md`](./docs/CLOUD_PROVIDERS.md). (The daemon-everywhere / continuous-build / local-execution era docs were deleted in July 2026 — see `docs/SESSIONS.md` history if you need them.)

1. **Cloud provider abstraction** — the seam is **done** and has two live providers: **Talyn Fleet** (`services/selfHosted/*` + `cloudProviders/selfhosted/provider.ts`, the default) and **PostHog Code**. **Codex Cloud is deferred** — OpenAI exposes no server-to-server cloud-task API (only the `codex cloud` CLI or `@codex` GitHub mentions); note this is a different thing from running **Codex on the fleet**, which works. Each provider is a self-contained `client + credentials + executor + poller + provider` module — no core changes. Selection is generic: `defaultCloudProvider` (`selfhosted | posthog_code | ask`) drives the backend resolver (`resolveCloudEnvChain`) and both front ends, with an "Ask every time" per-task **agent menu** on each PR row (the fleet contributes one entry per connected subscription, each carrying its model). Follow-ups: the deferred `TranscriptSource`/`TranscriptConverter` generalisation; measuring the real ChatGPT access-token lifetime, which decides whether server-side Codex refresh is worth its ToS exposure.
2. **Desktop polish** — the composer still has no freeform task entry; every task starts from a PR row or the skill picker. (The dead local-task UI — TaskFilesPanel/TaskGitPanel/awaiting_review flow — was removed in Session 52.)
3. **Phase 18.2 polish** — proper `talyn login` PKCE flow, CLI refresh-token rotation, invite flow.

**Recently landed**:
- Session 118 (**a pending failure is not an ejection**): trunk's "⚠️ … failed tests and is waiting for other pull requests to finish testing" was parsed as `failed`, so the queue fired a `queue_failure` run at a PR trunk still held. The run's push (usually a merge of master) ejected it and reset every PR behind it: 13 on PostHog/posthog#98918, 9 on #99003, six incidents in two days. It is `pending_failure` now, holding and push-would-eject, on both the comment and the `trunk-pending-failure` label channel. R5b/R5d, the submit ladder and the auto-keep watcher stand down until trunk retests the PR or removes it as `failed`. The bare `/has failed|failed tests/` rule also moved below every holding sentence: "waiting to start tests … because a pull request ahead of it failed tests" read as THIS PR failing, and the bisection and "running tests on this stack" lines matched nothing, which the REST backstop caches as "no state". **When trunk adds a sentence, ask whether it still has the PR before mapping it to an ejected state.** See Session 118 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 117 (**Workflows** — the PR automations the user writes): a top-level page, allow-listed to me, where a rule reads *on these PR events, matching these conditions, do these actions*, with a run history and derived stats so a rule that quietly stopped firing is visible. **The engine reads the webhook payload, never a PR row**, which is the only reading under which the human/bot author test and the `watch_pr` action mean anything — both are about PRs that are NOT already on your list — and it hooks in ABOVE `isRefreshEvent`, which answers the narrower "does this imply a refresh" (a `check_suite completed` passes it and is then a no-op). **Naming the gap beat defaulting it**: an `issue_comment` payload describes an *issue* (no base branch, no draft), so facts carry `unknownFields` and a condition on one FAILS rather than matching the empty default, with row enrichment tried first. **`UNIQUE (workflow_id, delivery_id)` is the whole idempotency design** — the run row is the claim, inserted before any action, because the worker's coalescing map is per-replica and "should I re-fetch" is not "should I post this comment". The PR is denormalised onto the run row so the history outlives an untracked PR and an un-watch; settling is two writes so a vanished task cannot strand a run at `running`. **Loop protection is two specific guards**: self-echo suppression on Talyn's own App (never the connected user, and `pr_merged` deliberately excluded — "comment when it merges" is a rule people want, and a merge cannot re-trigger a merge) plus a per-(workflow, PR) hourly cap counted from the table, announced once per window. **The merge action is the merge queue**, which forced the enqueue path out of `routes/pullRequests.ts` into `services/mergeQueue/membership.ts`. Refusals are normal and carry codes; `TaskLimitError` captures `paywall_deferred` server-side (Session 116's argument). Built on both front ends. Still open: the editor takes a raw skill key rather than using the picker, and there is no way to test-fire a workflow by hand. See Session 117 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 116 (**a whole client was analytically silent**): our #2 user by dispatched tasks had zero `paywall_shown` and zero `checkout_started` in three weeks, which read as a user ignoring the paywall. He is a contributor, and **`TALYN_POSTHOG_KEY` defaulted to `''` in both renderer webpack configs** — so any build made outside CI emitted no client analytics at all. Not that he was invisible: his release builds and his app.talyn.dev sessions reported 810 client events. The silent one was the build he runs alongside them, identifying as the committed `0.1.0` placeholder, and it is the client his automation drives — all twelve of his merge-queue enqueues came from it, none carrying a `merge_queue_toggled`. Every action that could have met a paywall was on the client that could not say so. The key is committed now (`apps/desktop/.erb/configs/posthogKey.ts`); a project write key is public by design, publish.yml already passed it as a repo **variable** rather than a secret, and `packages/mcp-server` has committed this exact key all along. **It had to move to `DefinePlugin` to work**: `EnvironmentPlugin` applies its default only when the var is `undefined`, and `apps/desktop/.env` is loaded before the plugins are built — so a bare `TALYN_POSTHOG_KEY=` line is *defined*, beat the default, and sent the build back to silent with nothing said (this happened on the first verification build). `resolvePostHogKey` computes it: disable flag, then a non-blank key, then the default. **A blank key is not an opt-out** — `TALYN_ANALYTICS_DISABLED=1` is, spelled as `packages/mcp-server` spells it. Separately, an unstamped build now reports **`dev+<sha>`** (`-dirty` when it applies) instead of a bare `dev`, so contributor builds are identifiable without ever parsing as a release, and **one predicate** (`renderer/lib/appVersion.ts`) answers "is this a release?" for both consumers — `useWhatsNew` tested `raw !== 'dev'`, which `dev+abc1234` sails straight through. **`environment` now reads the version, not `NODE_ENV`**: a locally packaged app is `NODE_ENV=production` and used to claim `environment: 'production'`, which was harmless only while those builds sent nothing. The bigger half was the silent deferral: 296 of his 298 dispatches never came from the UI, and a watcher that hits the task cap is **deferred server-side** with no request to answer — no 402, no modal, no event. His workspace has **5 auto-keep PRs, 0 queued, and `defaultAutoKeepMergeable = false`**, which makes all three paywalls unreachable by construction, while **143 of his 232 task starts happen with exactly 3 already in flight** — 5 armed PRs against 3 slots. Both deferral sites now capture **`paywall_deferred`** (`source: auto_keep | merge_queue` + `gate`/`limit`/`active`/`repo`/`pr_number`), server-side on purpose because the population that hits it hardest runs clients that report nothing. The queue's `deferred_task_limit` entry event stays — that is one entry's history, not the funnel. The deferral now also has a FACE: `autoMergeState.deferredSince` (carried by both `publicState` and the REST `publicAutoMergeState`) drives a **Waiting** chip on the PR row, and `useDeferredRuns` opens the UpgradeModal with its own reason `task_deferred` — titled **"A fix run was skipped"**, because the modal was not asked for and leading with the price reads as an ad. **Persisted rather than pushed**: a deferral has no request behind it and the user is usually not there (his last session was seven days before his last deferral), so a toast would interrupt an empty room. Bounded by BOTH once-per-launch (module-scope flag — a workspace switch remounts MainLayout) and once-per-24h (localStorage), and only while something is deferred right now. The `task_deferred` branch must precede the usage branches, inverting `auto_keep_default`'s reason: this user IS at the task limit, so `atTaskLimit` would answer "you're using all 3" to someone who never clicked. **Trap: `prAutoMergeWatcher`'s `readState` is an allow-list** — a new field is written to the jsonb and silently dropped on read; add fields in both places. Still open: `task_dispatched` records no dispatch source, and the merge-queue deferral has telemetry but no chip. See Session 116 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 115 (**one submission lands the whole stack**): a four-deep stack on posthog/posthog cost four full trunk test cycles (~40 min each) plus a retarget and often a paid rebase run per rung. trunk lands the rung it is given **plus every rung beneath it, atomically, in one round of CI**, so that is now what Talyn asks for. **Eligibility is GITHUB's signal, not ours** — `stack { id number size baseRefName }` + `stackEntry { position }` on the PR query (nullable, no preview header, `null` on a standalone PR, no extra round-trip); a branch-shaped `linkStack` chain still takes the serial drain, because trunk batches only what GitHub calls a stack. **The gate probe was asking the wrong branch**: a stacked PR's own base is the rung below it — an ordinary topic branch with no ruleset and no queue — so `getExternalMergeGate(…, entry.baseBranch)` answered "unguarded" for every rung above the bottom one; the gate that applies is `stack.baseRefName`'s (`prLandingBranch`), fixed in the queue, the auto-keep watcher, and `POST /pull-requests/:id/merge` (which was merging stack members into their parent's branch). **`external_covered_by` (migration `0051`) is the safety, not the mechanism** — the provider ejects the WHOLE batch when anything pushes to any member, so every rung below the submitted one is as untouchable as it is, and a covered rung has no provider comment and no gate of its own to say so. R4b gained a branch above the park: the submit rung falls through and submits itself on the STACK's gate, covered rungs park hands-off, and the rungs in between still **remediate** (a conflict four deep is real work that must happen before the submission) but never merge, arm, or submit (`stackBatchHoldsMerge`). The submission goes to the **TOP** rung and only when every rung is queued AND ready — top because lower lands a prefix; "every rung queued" because a submission lands rungs Talyn isn't tracking, so an unqueued rung would merge a PR nobody asked to merge. `services/repoStackBatching.ts` is the fourth `repoMergeGate`-shaped decaying tally: a `rejected` on a batched stack records the refusal and requeues for the serial drain instead of `blocked_manual`, and a `rejected` on an unstacked PR still blocks exactly as before. See Session 115 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 114 (**the fleet becomes the default, on your own subscription**): Talyn Fleet heads `CLOUD_PROVIDER_ORDER`; **Claude Code is deleted** (migration `0050` — which fails its in-flight tasks FIRST, because the poller skips a task whose provider is not registered, so those rows never fail, they hold a plan slot forever). Removing it exposed a bug it hid: `mergeablePromptVariables` branched only on `claude_code`, so every fleet run was told to publish with PostHog's `git_signed_*` tools, which do not exist in a microVM — the fleet has its own dialect now. **The custody hole**: `openaiKey: creds.openaiKey ?? ''` let the gateway fill a blank key from its own tenant's custody; it is a refusal now, plus per-vendor `policy.credentials` (exactly one entry — `allCredentialsSuppressed` nulls the refresh hook, and suppressing `github` takes away the clone token). Codex sign-in runs in the DESKTOP's main process because OpenAI's client redirects to `localhost:1455`, which a hosted backend can never be; the backend owns refresh only. `FLEET_MODELS` stopped being an alias of `POSTHOG_CODE_MODELS` — PostHog's `claude` runtime adapter 400s on a `gpt-*` id. See Session 114 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 111 (**three runs at one PR**): the auto-keep watcher dispatched five tasks at two PRs in five minutes; the duplicates filled the free plan's 3-active-task cap and starved a real fix run for six minutes, which the user saw as an unexplained paywall. **The cause was the DIRECTION of the task↔PR link** — `pull_requests.task_id` is one-to-one and every dispatch overwrites it, so "is anything already working this PR?" could only be answered for the last writer. **`tasks.pull_request_id` (migration `0048`) is now a real indexed FK**, written by `createCloudTask` with the row itself (not by the best-effort attach, whose failure is swallowed), and both dispatch paths guard on `activePrTaskId`. `ON DELETE SET NULL` — a task record outlives the tracking of its PR. Matching tasks by title was rejected: it breaks the moment a title is edited. **A watched PR now arms auto-keep** (`authored || explicitWatch`) so pasting a URL actually pushes; `reviewRequested` stays excluded because those arrive unbidden and in bulk. **The v1 merge-queue engine is deleted** (~3,080 lines) after six weeks and 722 merges on v2 — it still guarded on `task_id`, so a rollback would have reintroduced the bug. **Migration `0049` PINS the engine flag to `"v2"` rather than deleting the row**: the old instance in a deploy overlap reads an absent row as v1 and would wake its processor to drive the queue alongside v2, on different advisory locks. Still open: a run that never terminalises holds a plan slot forever, and `pull_requests.merge_queue_state` is still written by routes and read by MCP. See Session 111 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 110 (**"What's new", written by the commits**): a nightly release with an empty body told nobody what changed. The publish workflow now generates each release's user-facing highlights from its commits and posts them to `release_notes`; both front ends open a modal on the first launch after an update. **The unit is the SPAN, not the release** — `shouldShowWhatsNew` (`packages/shared/src/releaseNotes.ts`) returns everything between the version the client last showed and the one it is running, and `[]` unless something in it is worth reading, so a nightly cadence collapses into one modal or none. **Two filters, both load-bearing**: `filterReleaseCommits` drops merge commits, non-`feat|fix|perf` types and invisible scopes mechanically; Claude does the editorial pass on what survives. Generation is a `needs: [version, publish]` CI job (so a version that failed to build is never announced) that is `continue-on-error` and exits 0 on every soft failure — notes must never redden a shipped release; the `version` job gained a `previous` output because electron-builder has already created this release's tag by then. **The desktop passes a version ceiling and `apps/web` passes `null`** — the backend knows about tonight's release before the desktop user has it, whereas web is continuously deployed and always at or ahead of the cut — and `nextSeenVersion` is separate from what renders, because a release whose highlights were all for the other client is still SEEN. A build reporting `dev` opts out of the launch check entirely; Settings → About opens the modal regardless. `release_notes` is the one product table with no owner column, so both routes mount outside `ownerScope` and the ingest sits in the public block with a constant-time `X-Talyn-Release-Secret` (404 when unconfigured). A release with zero highlights still gets a row — that is what keeps `?since=` correct. See Session 110 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 107 (**PostHog Code run logs read the API that exists today**): a transcript that stopped or started partway through a run. PostHog's `session_logs` pages are capped by bytes as well as `limit` (`X-Has-More` is the only end-of-log signal, so page by `offset`), its Redis stream keeps only the newest 5,000 entries (a live attach now seeds from the durable log and tails with `?start=latest`), and its SSE announces rotation (`event: end`) and completion (`event: stream-end`), which the streamer now honours instead of feeding to the converter. A completed run's transcript is rebuilt from the durable log, persisted BEFORE a `task:update { transcript: [] }` reset tells the desktop to drop its seq-keyed copy. See Session 107 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 103 (**watch a PR you did not write**): a PR list that only ever showed PRs the poller DISCOVERED — `pollRepo`'s three searches, and the `authored` / `review_requested` booleans they produce — now takes an arbitrary PR by URL (`POST /pull-requests/watch`). It folds into **My PRs**, whose cohort is `authored || watching`, with a "Watching" toggle and a sky "Watched" row chip; watched PRs get the full action set. **`watching` (migration `0046`) is its own column and the monitor never writes it** — `reconcileRelationshipFlags` rewrites `authored`/`review_requested` every tick, so a faked `authored = true` would be cleared within a minute AND would arm the commit-pushing auto-keep-mergeable watcher on someone else's branch (`prCache` upsert insert path). Everything else was already free: `sweepClosed` skips a still-open PR that fell out of the searches, `applyPrResults` refreshes any row that exists, `getTrackedOpenNumbers` has no relationship predicate. **The WS field is optional and the client preserves it with `??` (never `||` — the un-watch echo sends `false`)**: every prCache upsert and flag reconcile emits `pull_request:updated` without it, so overwriting drops a just-watched PR off the list one tick later. A PR in an **unwatched repo** is a 409 `repo_not_watched` the modal asks about, then re-sends with `confirmAddRepo` — the check runs before any GitHub call, so the refusal costs zero API budget and no separate preflight endpoint is needed. Freshness: `filterStale` suppresses `untracked` for a watched row and `isCohortActive` counts it into `'mine'` (else it sat on the 300s slack TTL), and the add primes `noteHeadSha` so `check_run` events aren't dropped for the 60s until the head index reseeds. **The Reviews page carries its own watch button** (`POST /pull-requests/:id/watch { enabled }` — NOT the by-URL route: the row exists, so this is one column write and zero GitHub budget). Reviews is the only list where it earns a button: submitting a review clears `review_requested` and the PR leaves that list, so watching is what keeps it on My PRs — whereas an authored PR is already on My PRs and a queued one already on the queue. The stop-tracking face shows wherever a watched PR renders. **Un-watch cancels nothing** — `merge_queue_entries` cascades on the row, so it clears the flag and deletes the row only when nothing at all references it. Known cost, deferred: watching one PR in a big repo buys three search queries per tick forever (a `repositories.watch_only` flag is the fix). See Session 103 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 102 (**stop a task from the PR row**): the robot button's slot is one button with two faces. The robot starts a run; while the linked task is `pending`/`queued`/`in_progress` the same slot is a Stop button (`data-attr="pr-row-stop-task"`, calls `POST /tasks/:id/stop`), and it turns back into the robot once the task lands in `cancelled`, which the badge now reads as "Stopped". `/stop` cancels an undispatched task in place with a conditional update, so a dispatch racing the stop falls through to the remote cancel instead of being overwritten. PostHog runs are cancelled through the dedicated `POST .../runs/{id}/cancel/` action (interrupts the agent's turn, tears down the sandbox, cleans up a gone workflow) rather than `PATCH status=cancelled`. See Session 102 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 101 (**saved PR filters** + a Clear button): the PR list now carries the user's OWN named views, workspace-scoped in `settings.prFilters` so they follow the user between the desktop and app.talyn.dev. A filter tests repos (by `owner/repo` full name — a row id is re-minted when a repo is removed and re-added), labels (any/all + an exclude list), a case-insensitive title substring, and authors. **The matcher is `packages/shared/src/prFilters.ts`**, not a copy per front end — `apps/web` is a deliberate fork, so two predicates would let the same named filter show different PRs on each client. **Criteria AND within a filter; selected chips OR across** — they are views, so picking "Frontend" and "Backend" shows both rather than the empty intersection. A criteria-free filter is refused in the modal AND in `validatePRFilters`, because it would match every PR. Chips live on the filter bar's own second row (`GitHubPageShell`'s `filtersSecondary` slot) and count against the page cohort, not the filtered list. The `Clear` button resets every filter including the search box, and is hidden while nothing is active. Wired into My PRs + Reviews; the Merge Queue page is a queue-group view and was left alone. See Session 101 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 100 (**the skill picker asked GitHub 178 questions to list 88 skills**): repo-skill discovery walked one directory per request — 89 listings + 88 file reads on posthog/posthog, through an unbounded `Promise.all`. That shape is what GitHub's **secondary** limit counts (concurrent requests per ACCOUNT, the budget Session 97 is about), and once `githubRateGate` closes, `waitIfBlocked` throws on discovery's first call, so the picker showed "Couldn't load this repo's skills" for the whole backoff. Now: **one recursive git tree** (`getTreeRecursive`, `HEAD:<dir>?recursive=1`) replaces every per-directory listing — it needs `getDirectoryListingResolved` first, because the tree API addresses tree objects and `git/trees/HEAD:.claude/skills` 422s on posthog/posthog's symlink; **blob shas key a content cache** that outlives the listing TTL, so a re-discovery reads only the SKILL.md files that changed (warm = 2 calls); and the reads that remain share **one process-wide slot pool** (`CONTENT_CONCURRENCY` = 10 — a per-call bound still multiplies by the repo count under the desktop's prefetch-every-repo). Cold discovery: 178 calls → 91. The directory walk survives as the fallback for a truncated tree and as the ONLY way to read a symlinked skill dir. An error result is no longer cached (it kept the picker broken for the rest of the TTL), and `repoStatus: 'error'` now carries `repoError` so "GitHub rate-limited; retry in 285s" reads as itself. See Session 100 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 97 (**a merged PR that stayed on the list for an hour**): PostHog/posthog#87429 merged at 11:36Z and still read "Ready" with a live merge button at 12:15Z. The installation was inside GitHub's **secondary** GraphQL rate limit — which `githubRateGate` scopes `'all'`, because GitHub shares that throttle with REST — nine 300s backoffs in 41 minutes, largely earned by Talyn's own load. **Every path that drops a merged PR off the list was GraphQL-shaped and dropped the fact rather than deferring it**: the `pull_request/closed` webhook threw away the `merged`/`merged_at` it was HANDED to go ask GitHub instead (gated fetch throws → delivery acked → nothing retries); `sweepClosed` was the last step of `pollRepo`, so the most failure-prone step took the close-out with it; and the REST-only close-out (`sweepClosedViaRest`, zero GraphQL points) ran ONLY on the budget-reserve branch, never on a poll that failed. Now: `prMonitorService.markPrTerminal` writes terminal state from the payload BEFORE the refresh (**the only path that works under a shared gate — it makes no GitHub call**), `pollWorkspace` returns `{ failedRepos }` so the sweep can run the REST fallback on failure too, and a refused merge reconciles the row (`alreadyTerminal`) instead of reporting an error. The load that earned the gate was the auto-merge watcher's per-PR freshness refetch — one round-trip per watched PR per 60s tick against one shared budget — now one batched call per repo, skipped while that account is gated. See Session 97 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 94 (**the submit label goes out as the user**): trunk checks its two submit channels differently for the SAME App — it accepts `/trunk merge` from talyn-app[bot] ("Submitted to Merge by talyn-app[bot]", #85100) but answers that App's submit LABEL with "Only users that are a part of this repo's Trunk organization or have write permissions to the repo can submit a PR to the queue", then deletes the label. Door 2 therefore applies the label with `auth: 'user'` (the connected account has the write access trunk asks for); door 1 keeps the App, which works and is what trunk records as the submitter. Talyn's own tracking labels keep using the App. Still unverified that trunk accepts a user-applied label — the downside is bounded by Session 93's budget. See Session 94 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 88 (**back off a queue that is broken across PRs**): every guard was per PR, so a backlog of queued PRs each rediscovered a dead runner alone and spent its own budget doing it — and because trunk BATCHES, each of those submissions joined a batch that would fail and be bisected, lengthening the outage for the PRs already queued. **`services/repoQueueHealth.ts`** is the third instance of the `repoMergeGate.ts` / `repoSigning.ts` shape (a tally that decays and re-earns itself, no restart in any recovery path), scoped to `(repo, base)` and NOT to a workspace — following `externalQueueState`'s argument that this is a property of the repo, not of who is looking. Counted by DISTINCT PR rather than by failure, so one PR resubmitting cannot condemn a healthy queue, and one-sided like the classifier feeding it: only a positively identified `infrastructure` failure counts. It gates SUBMISSION only (a PR with a real blocker still gets its fix run, and a PR already in the queue is left alone), fires no notification (the whole point is that it hits many entries at once), and recovers on its own — the window ages out, and any merge clears the record. Thresholds are a first guess, exported for tuning. See Session 88 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 87 (**stop ejecting PRs from trunk's queue**): Talyn read every state trunk publishes and still did the one thing trunk punishes — push. A cloud fix run dispatched at a PR trunk was testing lands as a commit, and trunk answers `🚫 removed from the merge queue because it was pushed to by @x`, costing a whole test cycle plus the run that bought the ejection. The trigger was the everyday shape of a reviewed PR: `prNeedsFollowup` counts an unresolved review thread, so the "settled blocker" escape hatch in R5b was the common path, not an edge case. **Hands off now while the provider is OBSERVED holding the PR** (never merely on our own submission record, which would strand a PR whose provider goes quiet). **New rule R5d** states it on the provider's state rather than ours, so the routes R5b never covered — the author commenting `/trunk merge`, a PR queued after it was submitted, the desktop merge button — stop walking into the push and merge rules; gated bases only, because trunk's labels outlive it and a stale `trunk-testing` would otherwise wedge an ungated repo. The **keep-mergeable watcher** stood down only on `mergeQueued` (that's TALYN's queue) and now checks the gate + queue state too. Separately, a queue FAILURE is now acted on the FIRST ejection instead of being resubmitted unchanged: trunk batches, so a pointless resubmit costs a batch re-test plus a bisection to find the PR at fault again, with everything batched alongside it waiting; and the run is handed the checks trunk named (`failedChecks`, read off the failure table or the single-check link) instead of just "it failed tests". And the resubmit loop was unbounded in both bounds: `queueSignature` hashed trunk's verbatim sentence, which interpolates the pusher and the batch PR into exactly the reasons that repeat, so the recurrence guard never fired — it compares `externalQueueReason` now — while `submitAttempts` had been counted since it shipped and never read, despite the `external_queue_rejected` doc and the desktop's `submits: n/3` both promising a cap. See Session 87 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 86 (**two false "can't merge" alerts from the external queue**): trunk keeps ONE comment per PR and REWRITES it, so the `/trunk merge` instruction vanishes the moment trunk accepts the PR — which shut every submit door and blocked a queued PR as unmergeable. The ladder (`services/externalQueueSubmit.ts`) now reads the provider's comment FIRST and returns `already_submitted` when the queue is holding the PR, and the command is remembered per repo (`services/externalQueueSubmitRoute.ts`, fed by every comment list + `issue_comment` webhook) so a **resubmit** can still find the door. Separately, a queue run that dies in setup (a Docker port collision on posthog/posthog) is no longer read as the PR's fault: the parser keeps the Actions run trunk linked (`ExternalQueueStatus.failureUrl`), `services/externalQueueFailure.ts` classifies the job's failing steps, and an `infrastructure` verdict RESUBMITS instead of firing a `queue_failure` cloud run — bounded by `MAX_INFRA_SUBMITS_PER_HEAD`. **The classifier is one-sided on purpose**: anything it cannot positively recognise stays `unknown` and behaves exactly as before. See Session 86 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 85 (**merge stack** — drain a chain of dependent PRs): a stack is PRs where each one's base is the previous one's head, and the queue could not serialize them because its group key IS `(repo, base)` — every member was a group of one and all were simultaneously head. The queue now parks a child in **`awaiting_stack`** until the PR its base belongs to merges, then **retargets** it onto that PR's base (`PATCH /pulls/:n`) and returns it to the queue. The gate is an unconditional `decide` rule (**R4b**, between the draft rule and the auto-merge rule) because the group walk offers no protection across groups; its position matters in both directions — below R0/R1/R3 so a child that merged/closed/crashed mid-merge terminates rather than parks, above R5..R11 so a parked child never arms auto-merge, is submitted to trunk (which refuses stacks), fires a fix run, or merges. **`awaiting_stack` is in the auto-merge disarm invariant**: a parked child holding a Talyn arm gets merged by GitHub into its PARENT'S branch the instant checks pass. The parent edge is **derived per group walk, never persisted** (`services/mergeQueue/stack.ts`) — a stored edge would rot the way `base_branch` did, which it had been. A successful retarget **aborts** the evaluation rather than redeciding (signing/gate/auto-merge were probed against the old base) and writes the new base into the PR row's summary in the same breath (or the next reconcile flips the entry straight back). Enqueue is `POST /:id/merge-queue/stack`, resolved server-side from any member, **all-or-nothing against the free-plan cap**; dequeue always cascades upward. Linking lives in `packages/shared/src/stacks.ts` so the queue and both front ends cannot disagree. **Squash is the open hazard**: a squash-landed parent leaves its commits only on the child's branch, so `update_branch` conflicts and the cloud fix run does the rebase — it gets an explicit hint naming the parent and the new base. See Session 85 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 84 (**editable prompts**, Settings → Instructions): every prompt Talyn hands a cloud agent is rendered from a template in `packages/shared/src/promptTemplates.ts` (`{{variable}}` placeholders; provider-specific mechanics such as `{{gitRules}}` / `{{baseUpdateFlow}}` / `{{loopRules}}` are variables, so one template serves every provider). A workspace overrides a kind via `settings.prompts.<kind>` (validated on PATCH; `null` resets; `basedOnHash` flags when the shipped default moved on). Backend readers go through `services/promptTemplates.ts`; the desktop/web buttons read the workspace in the store. **When you change a shipped default, keep the variables intact** and let the hash do the rest. See Session 84 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 81 (**Connect with PostHog** — OAuth for PostHog Code, alongside the pasted key): PostHog is an OAuth2/OIDC AS whose tasks API accepts `pha_` tokens with the same scope/team enforcement as a personal key, and it supports **CIMD** — so `client_id` is a document we host (`apps/marketing/app/oauth-client/route.ts`), with no registration and no client secret (public client + PKCE). New connections lead with OAuth; **existing personal-API-key installs are never migrated or nagged** (`authMethod` absent reads as `personal_api_key`), and the key path stays the only option where `POSTHOG_OAUTH_CLIENT_ID`/`POSTHOG_OAUTH_REDIRECT_URI` aren't both set (all-or-nothing, the `POLAR_*` pattern). `required_access_level=project` + self-introspection means **the project id is no longer a form field**; a grant spanning an org or several projects is refused with a message, not guessed. The care is all in the token lifecycle: 1h access / 30d refresh, **rotation + reuse protection with a 120s grace**, so refreshes are single-flighted by an in-process promise map AND a *blocking* advisory lock (`posthog-oauth-refresh:<ws>`) — the loser must wait and re-read the rotated pair, since replaying a spent refresh token revokes the whole family. `invalid_grant` sets `oauth.reauthRequiredAt` ("Reconnect needed", nothing retries); a 5xx deliberately doesn't. PKCE states live in Postgres (migration `0040`), not a process-local Map, because the callback can land on the other instance mid-deploy. See Session 81 in [`docs/SESSIONS.md`](./docs/SESSIONS.md), [`docs/CLOUD_PROVIDERS.md`](./docs/CLOUD_PROVIDERS.md), and [`docs/SETUP.md`](./docs/SETUP.md) §6b.
- Session 80 (**admin.talyn.dev** — the operator console): a third browser app, `apps/admin`, gated on `users.is_admin`. Holds fleet ops (hosts / runs + transcripts / goldens / incidents), cross-tenant product admin (users / workspaces / tasks), the audit trail, and the Debug panel — **moved** out of `apps/web` and `apps/desktop`, not copied. Backend surface is one router at `/api/v1/admin`, mounted **pre-`ownerScope`** (under RLS the cross-tenant reads would return zero rows rather than erroring — a console that looks like it works and shows nothing). Reads DEGRADE (`probe()` never throws; an unreachable host is a row with a reason; a stale host is never dialled); mutations do NOT (an unreachable drain is a 502). Mutations are a stack of specific refusals rather than a roles table: reason (persisted verbatim), self-mutation guard, confirm-by-email, last-admin check in-transaction, and `TALYN_ADMIN_GRANT_ENABLED` **off by default**. Everything lands in `admin_audit_log` (migration `0039`) with two write shapes — `pending`-then-settle for remote calls that cannot be rolled back, one transaction for local ones. See Session 80 in [`docs/SESSIONS.md`](./docs/SESSIONS.md) and [`apps/admin/README.md`](./apps/admin/README.md).
- Session 75 (trunk's state comes from its COMMENT, not its labels): trunk keeps ONE comment per PR and **edits it in place** (instruction → "Submitted to Merge" → "Running tests" → "Merged successfully" / a 🚫❌⚠️ failure). Its status LABELS are optional in its config and were absent on every PR it was actively testing on posthog/posthog — so the label-only reading blocked seven healthy PRs with "the queue never picked it up". Parser: `externalQueueStatusFromComment` (`packages/shared/src/externalMergeQueue.ts`, pinned to verbatim bodies); cache: `services/externalQueueState.ts`, fed for free by the `issue_comment` webhook with a staleness-bounded REST backstop; `decide` prefers it over labels everywhere, blocks on "not picked up" only when trunk's submit checkbox is *observed* untouched, and **self-heals** (R5c) any external block the provider is seen contradicting. Persisted as `merge_queue_entries.external_state` (migration `0037`) → the desktop badge shows "Queue: testing" on a PR with no labels. See Session 75 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 74 (external merge queues — trunk.io): a base branch can be governed by a merge system Talyn can't merge past (posthog/posthog's `master` since July 2026 — a ruleset exempts only trunk.io's App, so our merge 405s "Cannot update this protected ref"). The queue now takes the PR to green and then **submits** it to that system, parks the entry in `awaiting_external`, tracks the provider's state (see Session 75 — its own comment, with labels as the fallback), and takes the PR back to fix + resubmit if it's ejected, bounded by a per-head submit budget. Submit ladder (`services/externalQueueSubmit.ts`): **the provider's own command** from its instruction comment (`/trunk merge` — the door that works at PostHog) → the repo's submit label → GitHub auto-merge (which serves GitHub's *native* queue; trunk ignores it). Both of the first two need the App's `issues: write`. Gate detection: `services/repoMergeGate.ts` (cheap branch-rules probe = `suspected`, observed 405 = `confirmed`). **Every gate decays** (Session 77): readings expire after ~5min and re-earn themselves from a fresh probe, one level at a time (`confirmed` → `suspected` → `null`), so a merge system that gets switched off no longer needs a backend restart to un-gate the repo. **A gated branch makes GitHub report `BLOCKED` for every PR**, so `decide` treats a bare BLOCKED as the gate, not a fixable blocker — the desktop's merge button / "Ready to merge" bucket do the same. Gated groups always evaluate eagerly — the provider does the ordering. See Session 74 in [`docs/SESSIONS.md`](./docs/SESSIONS.md).
- Session 71 (merge queue v2 — LIVE): the queue is now the event-driven pipeline in `packages/backend/src/services/mergeQueue/` (pure `decide()` core, `merge_queue_entries` + `merge_queue_events` tables, webhook-triggered per-(repo, base) group evaluation, 60s reconciler backstop, GitHub native auto-merge hybrid, self-healing per-headSha budgets). The old `mergeQueueProcessor.ts` is DORMANT but kept as the rollback target — flip `settings.merge_queue_engine` back to `"v1"` to roll back. Cleanup (delete v1 + drop the blob columns) is deferred until the v2 soak passes; see Session 71 in [`docs/SESSIONS.md`](./docs/SESSIONS.md) for the soak checklist.
- Session 52 (task-screen action audit): removed every button orphaned by the cloud-only refactor (Finish / Create PR / Reject & Requeue / Queue / Unqueue / retry-pr, the awaiting_review section + auto-commit banners, and the whole non-cloud Files/Git/Terminal tab branch); `awaiting_review` dropped from `TaskStatus`. Abort now remote-cancels the cloud run via the new optional `CloudTaskProvider.cancel()` (PostHog: PATCH the run to `cancelled`) and lands the task in `cancelled`, not `failed`.
- Session 43 (Inbox removal): ripped out the standalone Inbox end-to-end — `inbox_items` table (migration `0023`), `routes/inbox.ts`, `InboxPanel`, sidebar nav, shared `InboxItem*` types + `inbox:*` WS events, and the per-PR "unread updates" badges it powered (incl. `POST /pull-requests/:id/seen`). `prCache` still computes PR-event deltas + advances cursors; it just no longer materializes inbox rows. PRs needing attention now live only in the GitHub panel.
- Cloud-only refactor (June 2026): stripped the daemon, local/remote envs, in-process agents, permissions, backlog/continuous-build, and per-task git working tree. Built the pluggable `CloudTaskProvider` seam; made the task queue + poller cloud-only; collapsed the DB schema (dropped agents/backlog tables, slimmed environments to a marker, wiped tasks); removed the `@talyn/daemon` package. The app is now: PR dashboard + cloud task delegation.
- Session 17 (Phase 17.3 — notifications quick win): desktop OS notification fires when a task transitions into `awaiting_review`. Toggle + permission hint in Settings → Appearance → Notifications. Uses renderer `Notification` API — Electron bridges to the native OS surface.
- Session 17 (Phase 18.3.B): SSH auto-install. Desktop "Add Environment → Remote VM (Talyn daemon)" with two modes (auto-install over SSH, manual one-liner). Backend dials the target via ssh2, pipes `curl /daemon/install.sh | bash`, the script builds `@talyn/daemon` + writes a systemd/launchd unit, daemon pairs + dials back, modal polls for `connected`.
- Session 16 (Phase 18.3.B foundation): daemon relay layer + daemon envs first-class in scheduling + CI hygiene. Daemon runs a local HTTP proxy; child processes' REST calls tunnel over its WS. Backend accepts internal-auth headers in parallel with JWT. No user JWT on the VM. Scheduler/backlog fall back to any connected daemon when no env is pinned.
- Session 15 (Phase 18.3.A): daemon split foundation — new `packages/daemon`, `/daemon-ws` endpoint, `daemon` env type. Daemon can pair with the backend and proxy exec/spawn/git.
- Session 14 (Phase 18.4): backend deployed to Railway at `https://prod.talyn.dev`. Dockerfile + railway.toml + CI workflow. Desktop `.env` now points at hosted backend.
- Session 13 (Phase 18.2): end-to-end auth — Supabase GitHub OAuth, JWT middleware, `owner_id` scoping, RLS defense in depth, desktop login + CLI/MCP bearer tokens.

## File Structure

```
fastowl/
├── apps/
│   ├── desktop/                  # Electron desktop app
│   │   └── src/
│   │       ├── main/             # main + preload
│   │       └── renderer/         # React frontend (components, hooks, stores, lib)
│   ├── web/                      # @talyn/web — browser app (app.talyn.dev), Vite + React 19
│   └── admin/                    # @talyn/admin — operator console (admin.talyn.dev), Vite + React 19
├── packages/
│   ├── backend/                  # Express + WS server, DB, services
│   ├── cli/                      # @talyn/cli — `fastowl` binary
│   ├── client/                   # @talyn/client — REST + WS transport, shared by every front end
│   ├── mcp-server/               # @talyn/mcp-server — stdio MCP for child Claudes
│   └── shared/                   # Shared TS types
│   # (packages/daemon removed in the cloud-only refactor)
├── docs/                         # ARCHITECTURE, ROADMAP, SESSIONS, CLOUD_PROVIDERS, SETUP, etc.
├── supabase/                     # Local dev Supabase stack: `npm run dev:db` (config.toml +
│                                 # gitignored .env). Local dev must NEVER point at the prod
│                                 # DB / GitHub OAuth app — see docs/SETUP.md §0 for the why.
├── CLAUDE.md                     # This file
└── package.json                  # npm workspace root
```

Inside `packages/backend/src/`: `db/` (migrations + Drizzle schema/client), `routes/` (REST), `services/` (`taskQueue`, `cloudProviders/` (registry + poller + posthog/claude providers), `posthogCode/` (client/executor/streamer/converter), `claudeCode/` (client/credentials/executor/poller/converter — Anthropic Managed Agents, poll-based transcript), `github`, `prMonitor`, `prCache`, `taskPullRequest`, `events`, `websocket`), `__tests__/` (Vitest).

Inside `apps/desktop/src/renderer/components/`: `layout/`, `modals/`, `panels/`, `terminal/`, `widgets/`, `ui/` (shadcn).

**`apps/web` is a deliberate FORK of the desktop renderer, not a shared build of it.** Tom's call: every UI feature gets built twice from here on, in exchange for the two clients being able to diverge freely. What is NOT forked is the backend contract — both import `@talyn/client` — because two copies of that drift into runtime bugs rather than type errors. Three things the fork must keep straight, all verified with a spike before the app existed:
- **Env is `import.meta.env.VITE_*`, never `process.env.*`.** Vite's `define` entries are *"defined as globals during dev and statically replaced during build"*, so mirroring webpack's `EnvironmentPlugin` with a `define` of `process.env.TALYN_API_URL` serves the dev browser an unsubstituted expression that throws on the missing `process` global. `vite.config.ts` fails a production build outright when a required key is empty (a white screen on a public URL is much worse than the desktop's runtime throw) and refuses any value containing `service_role`.
- **OAuth is a full-page redirect** — `signInWithOAuth` with no `skipBrowserRedirect`, plus `detectSessionInUrl: true`. The desktop's `openExternal(data.url)` fires after two `await`s, so its `window.open` fallback has lost user activation and Safari/Firefox block it, silently, on the sign-in screen.
- **Never carry `migrateLegacyAuthFromLocalStorage` across.** On web the "bridge" IS localStorage, so its `setItem`-then-`removeItem` on the same key wipes the session every page load.

`packages/client` ships **dual-format** (`dist/cjs` + `dist/esm`, picked by the `exports` map) because Rollup cannot statically see the re-exports `tsc`'s CommonJS output emits as `Object.defineProperty(exports, …)` getters — Vite fails with *"not exported by"* — while the desktop's jest suite still needs CJS. Don't collapse it to one format without checking both.

**Browser-origin surface** (all inert until `app.talyn.dev` exists): `services/originPolicy.ts` is the one answer to "may this origin talk to us", shared by the REST CORS gate and the WS upgrade — **exact string match, never a pattern** (`ALLOWED_ORIGINS`), because a prefix/suffix rule is how `https://app.talyn.dev.evil.com` gets in. A rejected origin now denies by *omitting* the header (`cb(null, false)`) instead of throwing a 500, CORS is `credentials: false` (the API is Bearer-only, so CSRF-immunity is structural) with `maxAge: 86400` (the non-safelisted client-version header preflights every request). The `null`-origin concession for the packaged renderer's `file://` WS handshake is forgeable by any page and sits behind `TALYN_ALLOW_NULL_ORIGIN_WS` — **flip it to `0` the day anything moves to cookie auth**, or it becomes a live cross-site WebSocket hijack. `services/webApp.ts` owns `WEB_APP_URL`: read only from env, validated at boot, and `webAppUrl()` refuses any path that isn't single-slash-relative — it's the GitHub App callback's redirect target, and an open redirect there turns a login flow into a phishing hop. The callback ends per-client (browser → 302 home, desktop → close-this-tab page), decided by the Origin recorded server-side when the state was minted.

**`packages/client` is the single definition of the backend contract** — every route signature, every WS event type, the 401-refresh-and-replay, the reconnect backoff. Anything that talks to the backend imports it, so a route change can't be applied to one front end and forgotten in the other. It knows nothing about how a host stores a session or where its build-time env came from: hosts call `configureApiClient({ baseUrl, clientVersion, getAccessToken, recoverSession })` once at module scope. The desktop's binding is `apps/desktop/src/renderer/lib/api.ts` — ~50 lines of Supabase/`process.env` glue plus `export * from '@talyn/client'`, so the ~40 files importing `'../lib/api'` never had to move. **Add new endpoints here, not in a host app**, and remember it compiles to `dist` (`lib: ["ES2022", "DOM"]`), so it must be built before the desktop build, the typecheck, or jest.
