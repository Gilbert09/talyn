// ESM-only remark/rehype plugins (remark-gfm, rehype-raw). Stubbed as a
// no-op plugin for jest — the react-markdown stub ignores plugins anyway.
function noopPlugin() {}
module.exports = noopPlugin;
module.exports.default = noopPlugin;
